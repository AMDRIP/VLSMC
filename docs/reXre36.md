

---

# 🦖 Манифест языка reX (Rapid Execution / Native OS)

> **От кремния до квантовой механики — один язык. Без посредников.**

## 1. Фундаментальная проблема: Великий Раскол
Современное программирование страдает от "Великого Раскола". 
С одной стороны, у нас есть языки для **науки и данных** (Python, Julia, R, MATLAB). Они обладают прекрасным синтаксисом для математики, но работают в "тепличных условиях": они прячут железо за виртуальными машинами, сборщиками мусора и слоями библиотек (C/Fortran).
С другой стороны — языки для **систем и ядер** (C, C++, Rust, Zig). Они дают абсолютную власть над процессором и памятью, но писать на них сложные тензорные вычисления или пайплайны обработки сигналов — это боль, многословие и постоянная борьба с синтаксисом.

**reX** создан, чтобы уничтожить этот раскол. 

Это язык **"Полного спектра" (Full-Spectrum Language)**. Он позволяет вам написать загрузчик операционной системы, обработчик прерываний, а затем в том же самом файле прогнать данные с датчиков через тензорную математику с векторизацией SIMD. 

---

## 2. Пять столпов reX

### I. Нативная математика без накладных расходов
Векторы, матрицы и тензоры — это примитивы языка, а не внешние библиотеки. Компилятор понимает их структуру и автоматически применяет аппаратное ускорение (SIMD/AVX-512) или генерирует PTX-код для GPU.

### II. Физическая безопасность (Units of Measure)
Ошибки конвертации размерностей (например, метры вместо футов) приводили к крушению космических аппаратов. В reX физические величины проверяются на этапе компиляции. Если вы попытаетесь сложить секунды с килограммами, программа просто не скомпилируется.

### III. Управление памятью без мусорщика
Сборщики мусора (GC) недопустимы в ядре ОС и системах реального времени (Hard Real-Time). В reX используется ручное управление с умными абстракциями: `alloc`, `free`, гарантированное выполнение `defer`, а также модификаторы `view` (чтение) и `mut` (запись).

### IV. Бесшовный Bare-Metal
reX может генерировать "голый" машинный код. Язык поддерживает `inline asm`, `volatile` доступ к портам ввода-вывода (MMIO) и позволяет писать обработчики прерываний (ISR) без единой строчки на внешнем ассемблере.

### V. Метапрограммирование времени компиляции (Comptime)
Зачем считать в рантайме то, что можно посчитать до запуска? reX позволяет выполнять тяжелый код на этапе компиляции, встраивая в итоговый бинарник уже готовые массивы и константы.

---

## 3. Анатомия синтаксиса

### 🧬 Физические размерности на уровне типов
Забудьте о комментариях вида `// расстояние в метрах`. Пусть компилятор докажет законы физики за вас.

```rex
// Определение физических размерностей
unit kg;
unit m;
unit s;
alias Newton = kg * m / s**2;

fn calculate_force(mass: f64<kg>, accel: f64<m/s**2>) -> f64<Newton> {
    // Если попытаться вернуть mass + accel, компилятор выдаст ошибку!
    // Умножение kg * (m/s**2) автоматически дает тип <Newton>
    return mass * accel; 
}
```

### 🧮 Конвейеры данных (Pipelines) и Тензоры
Научный код должен читаться сверху вниз. Оператор `|>` передает результат левого выражения как первый аргумент в правую функцию. 

```rex
import math::linalg;

fn analyze_signal(raw_data: view<tensor<f64, 1>>) -> f64 {
    // broadcasting (.+) и пайплайны (|>) в действии
    let peak = raw_data 
        |> (s => s .+ 10.0)  // Смещаем сигнал (анонимная лямбда)
        |> linalg::fft       // Быстрое преобразование Фурье
        |> linalg::abs       // Модуль комплексных чисел
        |> linalg::max;      // Поиск максимума

    return peak;
}
```

### ⚙️ Уровень Ядра (Ring 0)
В reX вам не нужны прослойки. Вы можете обращаться к регистрам CPU и управлять железом напрямую.

```rex
@no_std
import core::types;

// volatile гарантирует, что оптимизатор не удалит эту запись в память
const VGA_BUFFER: ptr<mut u16> = 0xB8000 as ptr<mut u16>;

// Naked-функция (без пролога/эпилога) для обработки аппаратного прерывания
@interrupt
@align(16)
fn keyboard_isr() {
    let scancode: u8;
    
    // Ассемблерная вставка с безопасным биндингом переменных
    asm {
        "in al, 0x60"
        : out("al") => scancode
    }
    
    if scancode == 0x01 { // Нажат ESC
        volatile { VGA_BUFFER[0] = 0x0445; } // Пишем красную 'E' на экран
    }
    
    // Отправка EOI (End of Interrupt) в PIC
    asm { "out 0x20, al" : in("al") <= 0x20 }
}
```

---

## 4. Идеальный Симбиоз: Скрипт + Компилятор + ОС

**reX** — это не просто язык, это архитектура. Если вы пишете свою ОС, reX встраивается прямо в ядро в виде гибридной виртуальной машины (Bytecode VM) и JIT-компилятора.

**Как это работает на практике:**
1. Вы запускаете свою ОС. Появляется терминал.
2. Терминал вашей ОС — это REPL (Read-Eval-Print Loop) языка reX.
3. Вы вводите скрипт для расчета матрицы прямо в консоль. Интерпретатор reX внутри Ring 0 мгновенно выполняет его.
4. Вы решаете написать драйвер видеокарты или нейросетевой фильтр. Вы пишете функцию и помечаете её декоратором `@kernel`.
5. JIT-компилятор reX, зашитый в ядро, транслирует этот кусок AST-дерева в агрессивно оптимизированный x86_64 код, применяет AVX-512 инструкции и линкует его в живую систему без перезагрузки.

### Пример: Ядро, которое занимается наукой
Код ниже демонстрирует, как в одном файле уживаются обработка прерываний из Ring 0 и высокоуровневая математика с физическими типами.

```rex
import sys::hardware;
import math::linalg;

unit Volt;
const SENSOR_PORT: u16 = 0x400;

// Буфер без сборщика мусора
let data_buffer: tensor<f64<Volt>, 1> = alloc<f64<Volt>>(1024);
let idx: u32 = 0;

@interrupt
fn sensor_ready_isr() {
    let raw_val: f64<Volt>;
    asm { "in eax, dx" : in("dx") <= SENSOR_PORT, out("eax") => raw_val }
    
    data_buffer[idx] = raw_val;
    idx++;

    if idx == 1024 {
        // Мы накопили 1024 значения. Запускаем жесткую математику прямо из прерывания!
        // Декоратор fast отключает строгую проверку NaN/Inf для максимальной скорости
        fast {
            let energy = data_buffer |> linalg::fft |> linalg::mean;
            
            if energy > 100.0 {
                sys::panic("Критический всплеск энергии!");
            }
        }
        idx = 0;
    }
    
    hardware::ack_interrupt();
}
```

---

## 5. Дорожная карта (Roadmap)
- [x] Определение лексики и синтаксиса (Парсер рекурсивного спуска).
- [x] Интеграция в редакторы (Notepad++ UDL).
- [ ] Реализация Bytecode VM для интерпретации кода внутри Ring 0.
- [ ] Реализация типизации с физическими величинами (`comptime unit analysis`).
- [ ] Разработка минимального AOT-бекенда (генерация x86_64 без сторонних библиотек).
- [ ] Создание стандартной библиотеки: `math::linalg` (SIMD-матрицы) и `sys::os`.
- [ ] Интеграция JIT-компилятора reX в самописное ядро ОС.

## 6. Философия
Язык не должен диктовать программисту, что важно — скорость, безопасность или удобство. Инструмент должен быть достаточно гибким, чтобы стать скальпелем в операционной системе и суперкомпьютером в расчетах. 

**reX — это код, который подчиняет себе металл, чтобы понять вселенную.**


---

# 🦖 The reX Language Specification v1.0

**Статус:** Draft / Experimental  
**Цель:** Унификация системного программирования (OSDev) и научных вычислений (HPC).

---

## 0. Философия (The Zen of reX)

1.  **Никакой магии в рантайме.** В языке нет скрытых аллокаций, сборщика мусора или неявных вызовов системных библиотек.
2.  **Математика — это примитив.** Тензоры, матрицы и комплексные числа встроены в ядро языка, а не являются библиотечными классами.
3.  **Размерность имеет значение.** `5 метров` + `10 секунд` — это ошибка компиляции. Типы данных знают физику.
4.  **Явное лучше неявного.** Изменяемость (`let` vs `const`) и владение памятью должны быть видны сразу.
5.  **Один код — от ядра до кластера.** Один и тот же синтаксис используется для написания загрузчика ОС (16-bit real mode) и для обучения нейросетей (AVX-512/GPU).

---

## 1. Лексическая структура

### 1.1. Файлы и Кодировка
*   **Расширение:** `.rex`
*   **Кодировка:** UTF-8 (строго).

### 1.2. Комментарии
*   Однострочные: `// ...`
*   Многострочные (вложенные): `/* ... */`
*   Документирующие: `/// ...` (перед функцией/структурой).

### 1.3. Идентификаторы
Соответствуют регулярному выражению: `[a-zA-Z_][a-zA-Z0-9_]*`.
*   *Принятый стиль:* `snake_case` для переменных/функций, `PascalCase` для структур/типов, `UPPER_CASE` для констант.

### 1.4. Разделители
*   **Точка с запятой `;`** — обязательна в конце инструкций (statement terminator). Это упрощает парсинг и восстановление после ошибок.
*   **Фигурные скобки `{ }`** — обозначают блоки кода (scope).

---

## 2. Система Типов (Type System)

reX — язык со **строгой статической типизацией** и локальным выводом типов.

### 2.1. Примитивы
*   **Целые (знаковые):** `i8`, `i16`, `i32`, `i64`
*   **Целые (беззнаковые):** `u8`, `u16`, `u32`, `u64`
*   **С плавающей точкой:** `f16` (half), `f32` (float), `f64` (double), `f128` (quad).
*   **Комплексные:** `c32`, `c64`, `c128` (литералы: `1.0 + 2.0i`).
*   **Логический:** `bool` (`true`, `false`).
*   **Пустой:** `void` (для функций без возврата).

### 2.2. Научные примитивы
*   **Тензор:** `tensor<T, Rank>`
    *   Пример: `tensor<f64, 2>` (матрица), `tensor<f32, 1>` (вектор).
*   **Геометрические векторы:** `vec2`, `vec3`, `vec4` (хранятся в регистрах SIMD).
*   **Матрицы 4x4:** `mat3`, `mat4`.

### 2.3. Единицы измерения (Dimensional Types)
Синтаксис позволяет параметризовать числовые типы физическими юнитами.
```rex
unit kg; unit m; unit s;
let weight: f64<kg> = 75.0;
let speed: f64<m/s> = 10.0;
```

### 2.4. Системные типы (Memory Types)
*   **Сырой указатель:** `ptr<T>` (аналог `T*` в C). Поддерживает арифметику.
*   **Владеющий указатель:** `own<T>` (аналог `Box<T>` или `unique_ptr`). Освобождается при выходе из скоупа.
*   **Срез (View):** `view<T>` (указатель + длина). Безопасная ссылка на массив данных.
*   **Изменяемый срез:** `mut<T>` (указатель + длина + право на запись).

---

## 3. Объявление переменных и Констант

### 3.1. Неизменяемые (Const)
Значение должно быть известно на этапе компиляции или вычислено единожды.
```rex
const PI: f64 = 3.14159;
const MAX_PID: u32 = 65535;
```

### 3.2. Локальные привязки (Let)
Переменные по умолчанию **изменяемы** (в отличие от Rust, для простоты научных скриптов), но типы выиводятся.
```rex
let x = 10;      // Тип i32
let y: f64 = 0.5; // Явный тип
```

---

## 4. Функции и Методы

### 4.1. Определение функции
Ключевое слово `fn`. Возвращаемый тип указывается через `->`.
```rex
fn magnitude(v: vec3) -> f32 {
    return math::sqrt(v.x**2 + v.y**2 + v.z**2);
}
```

### 4.2. Модификаторы функций
*   `inline`: Подсказка встроить код функции.
*   `fast`: Разрешить небезопасные математические оптимизации (reordering, ignore NaN).
*   `comptime`: Функция выполняется во время компиляции.
*   `kernel`: Функция компилируется как ядро для GPU (CUDA/OpenCL).

### 4.3. Аргументы
Аргументы всегда передаются по значению, если не указано `ptr`, `view` или `mut`.

---

## 5. Управление потоком

### 5.1. Ветвление
```rex
if x > 0 { ... } else if x < 0 { ... } else { ... }
```

### 5.2. Циклы
*   **Бесконечный:** `loop { ... }`
*   **С предусловием:** `while condition { ... }`
*   **Итератор:** `for let i = 0; i < 10; i++ { ... }`

### 5.3. Сопоставление с образцом (Match)
Мощный инструмент вместо `switch`.
```rex
match value {
    0 => sys::print("Zero"),
    1..10 => sys::print("Small"),
    x if x > 100 => sys::print("Huge"),
    _ => sys::print("Unknown")
}
```

---

## 6. Операторы

### 6.1. Арифметические
`+`, `-`, `*`, `/`, `%` — стандартные операции.
`**` — возведение в степень (для научных расчетов).

### 6.2. Векторизованные (Element-wise)
Для тензоров и матриц операции, начинающиеся с точки, применяются к каждому элементу.
*   `A .* B` — поэлементное умножение.
*   `A * B` — матричное умножение.
*   `A .+ B` — поэлементное сложение.

### 6.3. Пайплайн (Pipeline)
Оператор `|>` передает результат левого выражения в первое звено правого.
```rex
data |> filter |> sort |> print;
```

---

## 7. Низкоуровневые возможности (Systems Programming)

### 7.1. Inline Assembly
Блок `asm` позволяет писать на целевом ассемблере архитектуры.
```rex
asm {
    "mov rax, 1"
    "int 0x80"
}
```

### 7.2. Volatile Memory
Блок `volatile` или тип `volatile` предотвращает оптимизацию доступа (для MMIO).
```rex
volatile { *VGA_PTR = 0xFF; }
```

### 7.3. Атрибуты (Decorators)
Начинаются с `@` и влияют на компиляцию/линковку.
*   `@no_std` — не подключать стандартную библиотеку.
*   `@packed` — не выравнивать поля структуры.
*   `@align(N)` — выравнивание памяти.
*   `@section(".text")` — указание секции линкера.
*   `@interrupt` — генерация `iret` вместо `ret`.
*   `@naked` — отсутствие пролога/эпилога функции.

---

## 8. Пример полной программы (Skeletal Structure)

```rex
// 1. Метаданные и импорты
@no_std
import sys::io;

// 2. Определение констант и юнитов
unit Hz;
const PI: f64 = 3.14159;

// 3. Структуры данных
export struct Signal {
    id: u32,
    data: tensor<f64, 1>
}

// 4. Реализация методов
impl Signal {
    fn new(size: u32) -> Signal {
        return Signal {
            id: 0,
            data: math::zeros(size)
        };
    }
}

// 5. Точка входа
fn main() -> i32 {
    let s = Signal::new(1024);
    
    // Научный синтаксис
    let freq: f64<Hz> = 50.0;
    
    // Системный вызов
    asm { "nop" };
    
    return 0;
}
```

---

## 9. Управление памятью и Resurces

### 9.1. Defer
Ключевое слово `defer` региструет блок кода, который выполнится при выходе из текущего скоупа (scope exit), независимо от того, как происходит выход (return, break или panic).
```rex
let ptr = alloc(1024);
defer free(ptr); // Выполнится в конце функции
```

### 9.2. Allocator API
Язык не навязывает модель памяти. Функции `alloc` и `free` являются частью модуля `memory`, который может быть переопределен пользователем (например, для использования внутри ядра).