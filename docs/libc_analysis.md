# Анализ готовности стандартной библиотеки C ([libc](file:///y:/DEV/VLSMC/user/libc/src/init.cpp#10-19)) VLSMC

Проведенный аудит директории `y:\DEV\VLSMC\user\libc\include` показал, что библиотека **находится на стадии уверенного MVP (Minimum Viable Product)**. У нас реализован ключевой фундамент, необходимый для написания простых системных утилит и базовых приложений, однако библиотека далека от полной POSIX/C99 совместимости.

Ниже представлен детальный разбор.

## 🟢 Что уже реализовано (Фундамент)

Библиотека предоставляет 9 заголовочных файлов, закрывающих самые критичные потребности:

### [string.h](file:///y:/DEV/VLSMC/user/libc/include/string.h) (Строки и память — Очень хорошо)
Это один из самых полных модулей.
- **Память:** [memcpy](file:///y:/DEV/VLSMC/kernel/src/libc.cpp#15-23), `memmove`, [memset](file:///y:/DEV/VLSMC/kernel/src/libc.cpp#24-31), `memcmp`
- **Строки:** [strlen](file:///y:/DEV/VLSMC/kernel/src/libc.cpp#32-39), `strcpy`, `strcat`, [strcmp](file:///y:/DEV/VLSMC/kernel/src/libc.cpp#40-47), `strchr`, `strrchr`, `strstr` 
- **Безопасные версии (C11):** `strnlen`, `strlcpy`, `strlcat`, `strncmp`, `memcmp_s`, `strncmp_s`

### [stdio.h](file:///y:/DEV/VLSMC/user/libc/include/stdio.h) (Ввод-вывод — Базовый уровень)
Реализована базовая структура `FILE` и потоковый I/O, но без сложного форматирования.
- Вывод в консоль: [printf](file:///y:/DEV/VLSMC/kernel/src/libc.cpp#376-453), `puts`, [putchar](file:///y:/DEV/VLSMC/kernel/src/libc.cpp#237-327)
- Ввод: [getchar](file:///y:/DEV/VLSMC/kernel/src/libc.cpp#11-14), `gets_s`
- Файловый I/O: [fopen](file:///y:/DEV/VLSMC/user/syscalls.h#145-148), [fclose](file:///y:/DEV/VLSMC/kernel/src/syscall_gate.cpp#618-631), [fread](file:///y:/DEV/VLSMC/user/syscalls.h#149-152), [fwrite](file:///y:/DEV/VLSMC/kernel/src/syscall_gate.cpp#594-617), `fgetc`, `fputc`, `feof`, `ferror`, `fflush`
- Примитивы блокировок потоков: `flockfile`, `funlockfile`

### [stdlib.h](file:///y:/DEV/VLSMC/user/libc/include/stdlib.h) & [malloc.h](file:///y:/DEV/VLSMC/user/libc/include/malloc.h) (Общего назначения — Средне)
- **Аллокатор памяти:** Базовые [malloc](file:///y:/DEV/VLSMC/user/app_api.h#136-139), `calloc`, `realloc`, [free](file:///y:/DEV/VLSMC/user/app_api.h#140-143)
- **Математика/Конвертация:** [atoi](file:///y:/DEV/VLSMC/user/libc/src/stdlib.cpp#15-35), [itoa](file:///y:/DEV/VLSMC/user/libc/src/stdlib.cpp#48-85) (нестандартн.), [abs](file:///y:/DEV/VLSMC/user/libc/src/stdlib.cpp#86-89), [labs](file:///y:/DEV/VLSMC/user/libc/src/stdlib.cpp#90-93), [div](file:///y:/DEV/VLSMC/user/libc/src/stdlib.cpp#94-105), [ldiv](file:///y:/DEV/VLSMC/user/libc/src/stdlib.cpp#106-117), [rand](file:///y:/DEV/VLSMC/user/libc/src/stdlib.cpp#118-122), [srand](file:///y:/DEV/VLSMC/user/libc/src/stdlib.cpp#123-126)
- **Процессы:** [exit](file:///y:/DEV/VLSMC/user/app_api.h#13-17), [abort](file:///y:/DEV/VLSMC/user/libc/src/stdlib.cpp#11-14)
- *Примечание:* Здесь также лежат [fork](file:///y:/DEV/VLSMC/user/libc/src/stdlib.cpp#127-130), [exec](file:///y:/DEV/VLSMC/user/app_api.h#68-71), [execve](file:///y:/DEV/VLSMC/user/libc/src/stdlib.cpp#131-134), [wait](file:///y:/DEV/VLSMC/user/libc/src/stdlib.cpp#141-144), которые по стандарту POSIX должны находиться в `unistd.h` или `sys/wait.h`.

### [math.h](file:///y:/DEV/VLSMC/user/libc/include/math.h) (Математика — Минимально)
- Тригонометрия: `sin`, `cos` (и `f`-версии)
- Корни и степени: `sqrt`, `pow`
- Модуль числа: `fabs`

### Прочее
- [errno.h](file:///y:/DEV/VLSMC/user/libc/include/errno.h): Готов полностью (определено 38 стандартных кодов ошибок и макрос `errno`).
- [stdint.h](file:///y:/DEV/VLSMC/user/libc/include/stdint.h), [stddef.h](file:///y:/DEV/VLSMC/user/libc/include/stddef.h), [stdarg.h](file:///y:/DEV/VLSMC/user/libc/include/stdarg.h): Присутствуют и содержат стандартные типы/макросы (`size_t`, `va_list` и т.д.).
- [sys/syscall.h](file:///y:/DEV/VLSMC/user/libc/include/sys/syscall.h), [sys/mutex.h](file:///y:/DEV/VLSMC/user/libc/include/sys/mutex.h): Системные обертки специфичные для VLSMC.

---

## 🔴 Чего не хватает (Пробелы)

Для портирования сторонних C-программ (таких как `zlib`, `lua` или даже простых игр) библиотеке сильно не хватает следующих модулей:

### 1. Отсутствующие стандартные заголовочные файлы
- **`ctype.h`**: ВООБЩЕ НЕТ. Отсутствуют функции проверки и смены регистра (например, `isalpha`, `isdigit`, `toupper`, `tolower`).
  > **Реализация:** Создать статический массив `static const unsigned char _ctype_[257]` с битовыми флагами (IS_DIGIT, IS_UPPER, etc.). Функции реализуются как макросы или инлайны: `return _ctype_[c+1] & FLAG;`. Это стандартный и самый быстрый способ (O(1)).
- **`time.h`**: ВООБЩЕ НЕТ. Невозможно получить текущее время ([time()](file:///y:/DEV/VLSMC/user/app_api.h#40-43)), форматировать дату (`localtime`), или замерить тайминги (`clock()`).
  > **Реализация:** `time()` — обертка над сисколлом `sys_get_time` (Unix timestamp). `localtime()` — чистая математика перевода timestamp в `struct tm` (пока только UTC). `clock()` — требует сисколла `sys_uptime` (тики таймера с момента загрузки).
- **`assert.h`**: Нет стандартного макроса утверждений `assert()`.
  > **Реализация:** Макрос `#define assert(expr) ...`, который при `!(expr)` вызывает `fprintf(stderr, "Assertion failed: %s, file %s, line %d\n", #expr, __FILE__, __LINE__)` и затем `abort()`.
- **`stdbool.h`**: Нет определения типа `bool` для C (хотя для C++ это не нужно).
  > **Реализация:** Простой хедер: `#define bool _Bool`, `#define true 1`, `#define false 0`, `#define __bool_true_false_are_defined 1`.
- **`limits.h` / `float.h`**: Нет констант ограничений типов (например, `INT_MAX`).
  > **Реализация:** Использовать встроенные макросы компилятора GCC: `#define INT_MAX __INT_MAX__`, `#define FLT_MAX __FLT_MAX__`. Это гарантирует корректность для любой архитектуры.
- **`unistd.h`**: Нет стандарта POSIX. Многие системные вызовы сейчас смешаны в [stdlib.h](file:///y:/DEV/VLSMC/user/libc/include/stdlib.h).
  > **Реализация:** Перенести прототипы `read`, `write`, `close`, `fork`, `exec`, `sbrk` из `stdlib.h` и `syscalls.h` сюда. Добавить типы `ssize_t`, `pid_t`.
- **`dirent.h`**: Нет стандартного механизма обхода директорий.
  > **Реализация:** Структура `DIR` (содержит fd и буфер). `opendir` делает `open(path, O_DIRECTORY)`. `readdir` вызывает сисколл `sys_getdents` (get directory entries), заполняя буфер ядра и возвращая указатель на `struct dirent`.


### 2. Отсутствующие функции в существующих файлах
- **В [stdio.h](file:///y:/DEV/VLSMC/user/libc/include/stdio.h)**: 
  - Нет функций форматирования строк: `sprintf`, `snprintf`, `vsprintf`.
  - Нет записи в файлы с форматом: `fprintf`, `vfprintf`.
  - Нет парсинга: `scanf`, `fscanf`, `sscanf`.
  - Нет навигации по файлу: `fseek`, `ftell`, `rewind`.
- **В [stdlib.h](file:///y:/DEV/VLSMC/user/libc/include/stdlib.h)**:
  - Нет сортировки и поиска: `qsort`, `bsearch`.
  - Нет конвертации строк (только базовый atoi): `strtol`, `strtoul`, `strtod`.
  - Нет доступа к окружению: `getenv`, `setenv`.
- **В [string.h](file:///y:/DEV/VLSMC/user/libc/include/string.h)**:
  - Нет токенизации и поиска наборов: `strtok`, `strpbrk`, `strspn`, `strcspn`.
  - Нет получения строки по коду ошибки: `strerror`.
- **В [math.h](file:///y:/DEV/VLSMC/user/libc/include/math.h)**:
  - Отсутствуют `tan`, `asin`, `acos`, `atan`, [exp](file:///y:/DEV/VLSMC/test_run.exp), [log](file:///y:/DEV/VLSMC/out.log), `log10`, `ceil`, `floor`, `fmod`.

## 💡 Вердикт и план развития
Сейчас [libc](file:///y:/DEV/VLSMC/user/libc/src/init.cpp#10-19) достаточна для написания драйверов и системных утилит VLSMC. Однако, если цель — запускать сторонний софт или иметь полноценный POSIX-слой, первоочередными задачами должны стать:
1. Реализация семейства `sprintf` / `snprintf` в `stdio`.
2. Добавление `ctype.h` (это делается за 10 минут массивами-таблицами).
3. Добавление `time.h` (связывание с системным RTC драйвером).
4. Добавление `fseek`/`ftell` для полноценной работы с файлами.
