# RAND Elecorner 36 — План Проекта

> **Ретроспектива и Roadmap разработки Bare-Metal ОС**
> Build 0001 (x86 32-bit Protected Mode)

---

## О проекте

**RAND Elecorner 36** — 32-битная операционная система, написанная с нуля. Переход от концептуального Qt-симулятора к физической архитектуре потребовал пересмотра всех этапов разработки. Делаем ставку на гибридно-микроядерную архитектуру с развитым пользовательским пространством (Ring 3) и полноценной поддержкой динамической линковки.

### Стек технологий

| Компонент | Технология |
|-----------|-----------|
| Загрузчик | **NASM** (16-bit to 32-bit) |
| Ядро ОС | **C++17** (Freestanding, no STL) |
| Сборка | **GCC (Cross-Compiler)**, **mtools**, **Bash** |
| Тестирование | **QEMU**, **Bochs** |

> Подробнее: [TECH_STACK.md](TECH_STACK.md)

---

## Этап 1 — Bootstrap и Базовое Ядро

| Статус | Задача | Описание |
|---|--------|----------|
| ✅ | 1.1 First Stage Bootloader | Чтение FAT из загрузочного сектора, переход ко второй стадии. |
| ✅ | 1.2 Second Stage Bootloader | Линия A20, карта памяти (E820), парсинг FAT16, загрузка `KERNEL.BIN`. |
| ✅ | 1.3 Переход в Protected Mode | Настройка локальной GDT и прыжок в 32-битный режим. |
| ✅ | 1.4 С++ Kernel Entry | Стек ядра, вызов `_init()` конструкторов, `kernel_main`. |
| ✅ | 1.5 Базовый вывод (VGA) | VGA Text Mode Driver (0xB8000). |
| ✅ | 1.6 Глобальная GDT/IDT | Векторы прерываний, обработка исключений (Page Fault, #GP, Panic). |

---

## Этап 2 — Управление Памятью (Memory Subsystem)

| Статус | Задача | Описание |
|---|--------|----------|
| ✅ | 2.1 PMM (Physical Memory) | Менеджер физической памяти на основе Bitmap (кадры по 4Кб). |
| ✅ | 2.2 VMM (Virtual Memory / Paging) | Page Directory, Page Tables, маппинг ядра. |
| ✅ | 2.3 Heap Allocator (kmalloc) | Реализация кучи ядра для поддержки оператора `new`. |
| ✅ | 2.4 C++ Runtime / libc | Заглушки для минимальной поддержки C++ (new/delete, `__cxa_pure_virtual`). |

---

## Этап 3 — Interrupts & Hardware Abstraction (HAL)

| Статус | Задача | Описание |
|---|--------|----------|
| ✅ | 3.1 Контроллер прерываний (PIC)| Переназначение IRQ (0-15). |
| ✅ | 3.2 Таймер (PIT) | Системный тик на IRQ0 для планировщика. |
| ✅ | 3.3 Драйвер клавиатуры | Обработчик `IRQ1` на порту `0x60`, конвертация Scan Code в ASCII. |
| ✅ | 3.4 RTC (Real Time Clock) | Чтение времени из CMOS. |
| ✅ | 3.5 PCI Bus Scanning | Сканирование шины PCI (`class_id`, `vendor_id`). |

---

## Этап 4 — Многозадачность и Планировщик

| Статус | Задача | Описание |
|---|--------|----------|
| ✅ | 4.1 Структуры данных | `TCB` (Thread Control Block) / процессы. |
| ✅ | 4.2 Сохранение контекста | Ассемблерный макрос `switch_task`. |
| ✅ | 4.3 Kernel Threads | Потоки, работающие в Ring 0. |
| ✅ | 4.4 Round Robin Scheduler | Вытесняющая многозадачность по таймеру. |

---

## Этап 5 — Переход в Пользовательское Пространство (Ring 3)

| Статус | Задача | Описание |
|---|--------|----------|
| ✅ | 5.1 TSS (Task State Segment) | Возврат из Ring 3 в Ring 0 (указатель `esp0`). |
| ✅ | 5.2 Загрузчик форматов ELF | Парсинг 32-битных исполняемых файлов (статика). |
| ✅ | 5.3 Изоляция процессов | Личная `Page Directory` для каждого процесса (Ring 3). |
| ✅ | 5.4 Syscall Gate (`int 0x80`)| Интерфейс системных вызовов (`eax`, `ebx`, `ecx`). |
| ✅ | 5.5 `libc` / `user_crt0` | Пользовательский runtime (включает свой `malloc`, `stdio`, строковые функции). |

---

## Этап 6 — Межпроцессное Взаимодействие (IPC)

| Статус | Задача | Описание |
|---|--------|----------|
| ✅ | 6.1 Архитектура обмена | Поддержка сообщений между процессами. |
| ✅ | 6.2 Синхронные/Асинхронные | `send_message`, `receive_message`. |
| ✅ | 6.3 Blocking IPC (Wait/Wake)| Перевод потока в "Wait" и пробуждение. |
| 🚧 | 6.4 Сигналы и Пайпы | POSIX-подобные pipes для Shell redirection, асинхронные сигналы (в планах). |

---

## Этап 7 — Файловые Системы (File System VFS)

| Статус | Задача | Описание |
|---|--------|----------|
| ✅ | 7.1 ATA/AHCI Драйверы | Прямое чтение секторов с диска. |
| ✅ | 7.2 Парсер FAT16 | Чтение ФС, директорий и файлов. |
| ✅ | 7.3 Абстракция VFS | Единый интерфейс (vnode) — `open()`, `read()`, `write()`, `close()`, refcounting. |
| 🚧 | 7.4 Поддержка записи ФС | Модификация FAT, выделение кластеров, создание новых вложенных файлов файлов/директорий (частично поддерживается). |
| ✅ | 7.5 Буферизованный I/O в libc | Реализация высокоуровневого ввода-вывода (`fopen`, `fread`, `fclose`, буферизация в user-space). |

---

## Этап 8 — Shell / Терминал ОС

| Статус | Задача | Описание |
|---|--------|----------|
| ✅ | 8.1 Модуль Shell | Разбор введенных строк. |
| ✅ | 8.2 Встроенные команды | `help`, `clear`, `ls`, `cat`, `ps`, `mem`, `time`, `pci`, утилиты для тестирования. |
| ✅ | 8.3 Запуск приложений | Запуск произвольных `.elf` из файловой системы. |
| 🚧 | 8.4 Redirection (I/O) | Перенаправление ввода-вывода (`>` и `>>`, поддержка pipe `|`). |

---

## Этап 9 — Динамическая Линковка и Shared Objects (NEW)

| Статус | Задача | Описание |
|---|--------|----------|
| ✅ | 9.1 Memory Mapping (`mmap`) | Полная реализация `sys_mmap` / `sys_munmap` (анонимная память, MAP_PRIVATE file-backed). |
| ✅ | 9.2 Page Caching для .SO | Кэширование физических кадров (phys_frame) read-only страниц файлов (vnode+offset) для экономии памяти, интеграция с demand paging. |
| ✅ | 9.3 Поддержка `PT_INTERP` | Парсинг `PT_INTERP` в ядре, загрузка приложения + Runtime Linker, формирование вектора `auxv`. |
| ✅ | 9.4 Runtime Linker (`ld.so`) | User-space линкер: scope chain (`link_map`), парсинг `DT_NEEDED`, загрузка библиотек по имени, кросс-библиотечный `lookup_symbol`. |
| ✅ | 9.5 Динамические программы | Разделяемые библиотеки (`.so`) и PIE-программы, полноценное функционирование GOT/PLT механизмов. |

---

## Этап 10 — Продвинутые Драйверы (AHCI & DMA)

| Статус | Задача | Описание |
|---|--------|----------|
| 🚧 | 10.1 AHCI Контроллер | Инициализация HBA, обнаружение SATA портов, отказ от IDE/ATA PIO. |
| 🚧 | 10.2 DMA / Bus Mastering | Реализация PRDT (Physical Region Descriptor Table) для прямого доступа к памяти без участия CPU. |
| 🚧 | 10.3 MSI Прерывания | Настройка Message Signaled Interrupts для PCI устройств. |
| 🚧 | 10.4 LBA48 | Поддержка дисков большого объема (>128 ГБ). |

---

## Этап 11 — Сетевой Стек (Networking)

| Статус | Задача | Описание |
|---|--------|----------|
| 🚧 | 11.1 Драйвер NIC | RTL8139 или Intel E1000 (PCI), управление кольцевыми буферами RX/TX. |
| 🚧 | 11.2 L2 Ethernet | Обработка MAC-адресов и Ethernet-кадров. |
| 🚧 | 11.3 L3 IPv4 & ARP | IP-адресация, ARP-таблица, ICMP (Ping). |
| 🚧 | 11.4 L4 UDP/TCP | Стейт-машина TCP, управление окном, ретрансмиссия, UDP сокеты. |
| 🚧 | 11.5 Socket API | Системные вызовы `socket`, `bind`, `connect`, `send`, `recv` (BSD-style). |

---

## Этап 12 — Графическая Подсистема (GUI)

| Статус | Задача | Описание |
|---|--------|----------|
| 🚧 | 12.1 VESA / BGA | Переключение видеорежимов (1024x768x32) через VBE или BGA (QEMU). |
| 🚧 | 12.2 Фреймбуфер | Реализация Double Buffering, примитивы рисования (линии, прямоугольники, битмапы). |
| 🚧 | 12.3 PS/2 Мышь | Драйвер мыши, обработка пакетов перемещения и нажатий (IRQ 12). |
| 🚧 | 12.4 Оконный Композитор | Менеджер окон, Blitting буферов приложений на экран, курсор. |

---

## Этап 13 — Стабилизация и POSIX

| Статус | Задача | Описание |
|---|--------|----------|
| 🚧 | 13.1 Сигналы | Реализация `kill`, `sigaction`, обработка `SIGINT` (Ctrl+C), `SIGCHLD`. |
| 🚧 | 13.2 Pipes & Redirection | Каналы для межпроцессного взаимодействия (`|`), перенаправление stdio (`>`, `<`). |
| 🚧 | 13.3 Окружение (Env) | Переменные окружения `envp` для процессов. |
| 🚧 | 13.4 Портирование ПО | Запуск Doom, GCC или Python на базе расширенной libc. |
